# ShopLedGer — সম্পূর্ণ অডিট রিপোর্ট (হালনাগাদ)

**তারিখ:** ১৮ সেপ্টেম্বর ২০২৬
**শাখা:** `arena/01a0b525-shopledger`
**বেস কমিট:** `ed1a227` (main)
**প্রকল্প সংস্করণ:** v0.1.0

এই রিপোর্টে (১) পুরনো অডিটের আইটেমগুলোর বর্তমান অবস্থা, এবং (২) নতুন করে পাওয়া সমস্যা ও
এই ব্রাঞ্চে করা ফিক্স — দুটোই অন্তর্ভুক্ত। শেষে টুল-ভেরিফিকেশনের ফলাফল দেওয়া আছে।

---

## ✅ টুল-ভেরিফিকেশন সারসংক্ষেপ

| চেক | ফলাফল |
|-----|--------|
| `npm ci` / `npm install` | ✅ সফল (আগে **ব্যর্থ** — peer-dep কনফ্লিক্ট, দেখুন N1) |
| `npm run build` (tsc + vite) | ✅ সফল — precache 4198 KiB → **1338 KiB**, মূল JS 1076 kB → **~500 kB** (PDF লাইব্রেরি lazy chunk) |
| `npm test` | ✅ **৭১/৭১** পাস (৪টি নতুন: প্যাড/লেটারহেড) |
| `npm run test:ui` (happy-dom স্মোক) | ✅ **৪২/৪২** — প্যাড কেন্দ্রে, প্রিভিউ পপ-আপ, রসিদের প্যাড |
| `npm run lint` (ESLint) | ✅ ০ এরর, ০ ওয়ার্নিং (আগে eslint ইনস্টলই ছিল না — N2) |
| `npm audit` | ✅ ০ ভালনেরেবিলিটি |

---

## 🖨️ প্রতিষ্ঠানের প্যাড (পেজের মাঝখানে) + রিপোর্ট প্রিভিউ পপ-আপ — নতুন

**১) প্যাড সব জায়গায়, মাঝখানে:** লোগো, প্রতিষ্ঠানের নাম, ঠিকানা ও ফোন এখন **সব জায়গায়
পেজের কেন্দ্রে** বসে — A4 রিপোর্ট শিট, রিপোর্ট প্রিভিউ, ক্রেতার হিসাব বিবরণী, বিক্রি রসিদ ও
লেনদেনের রসিদ (টাকা আদায়/পরিশোধ)।

- নতুন শেয়ারড কম্পোনেন্ট `src/components/org/PadHeader.tsx` (inline style — html2canvas
  ক্যাপচারে স্টাইল হারায় না) — `size: 'sheet' | 'screen' | 'receipt'`, নিচে দাগ (solid/dashed),
  শাখার লাইন ও সময়কাল।
- প্যাডের তথ্য `src/lib/orgPad.ts` (`orgPadOf(branch)`) থেকে — মালিক **শাখা ও ব্যবস্থাপক →
  প্যাড** ট্যাবে যা লেখেন সেটাই যায় (প্রতিষ্ঠানের নাম + শাখার নাম + ঠিকানা + ফোন + লোগো)।
- ওই ট্যাবেই এখন **লাইভ প্যাড প্রিভিউ** — লিখতে লিখতেই দেখা যায় মাঝখানে কেমন বসবে।
- প্যাড সেট না থাকলে রিপোর্ট/লাভ-ক্ষতি পেজে মনে করিয়ে দেওয়ার বার্তা (`PadEmptyHint`)।

**২) আগে রিপোর্ট, পরে ডাউনলোড (কোনো অকারণ ডাউনলোড নয়):** সব জায়গায় ফ্লো এখন
**ফিল্টার → রিপোর্ট তৈরি → প্রিভিউ পপ-আপ → সেইখান থেকে ডাউনলোড/শেয়ার**।

- নতুন `src/components/report/ReportPreviewModal.tsx` — সম্পূর্ণ রিপোর্ট পপ-আপে (স্ক্রল করা যায়),
  নিচের বারে **PDF ডাউনলোড** ও **শেয়ার / WhatsApp** (Web Share API-তে আসল PDF ফাইল, না থাকলে
  ডাউনলোড + `wa.me` টেক্সট), Escape/× দিয়ে বন্ধ, বন্ধ না করলে কিছুই ডাউনলোড হয় না।
- `ReportPreview` বাড়ানো হয়েছে: প্রিভিউতে আর ৮টি নয়, **প্রথম ৪০০ সারি** (বাকিটা PDF-এ), আর
  অ্যাকশন বোতামগুলো পপ-আপের বারে সরানো হয়েছে — কার্ডে ও পপ-আপে প্যাড একই দেখায়।
- প্রযোজ্য: **রিপোর্ট সেন্টার** (১০টি রিপোর্ট + দ্রুত সারসংক্ষেপ/QuickSummary), **লাভ-ক্ষতি**,
  **ক্রেতার হিসাব বিবরণী**, **বিক্রি রসিদ**, **লেনদেনের রসিদ**। রসিদ দুটি আগেই পপ-আপ ছিল —
  এখন সেগুলোতেও প্যাড + প্রিভিউ + ডাউনলোড/শেয়ার/প্রিন্ট একসাথে।
- **ঝকঝকে PDF মোবাইলেও:** ক্যাপচারের সময় প্রিভিউ-এলিমেন্টটি সাময়িকভাবে A4 প্রস্থে (৭১৮px)
  বসে (`data-pdf-width` — `src/lib/reportExport.ts`), আর ওই মুহূর্তে `PdfBusyOverlay` স্ক্রিন
  ঢেকে রাখে — তাই লাফালাফি চোখে পড়ে না।
- নতুন ফাইল: `src/lib/orgPad.ts`, `src/components/org/PadHeader.tsx`,
  `src/components/report/ReportPreviewModal.tsx`, `src/components/report/PdfBusyOverlay.tsx`,
  `tests/orgPad.test.ts` (৪টি টেস্ট)।
- **যাচাই:** `npm run build` ✅, `npm run lint` ০ এরর ✅, `npm test` **৭১/৭১** ✅,
  UI স্মোক **৪২/৪২** ✅ (প্যাড কেন্দ্রে আছে কি না, প্রিভিউ পপ-আপ খোলে কি না, রসিদে
  নাম-ঠিকানা-ফোন আছে কি না — সব যাচাই করা হয়)।

---

## 📄 রিপোর্ট সেন্টার v2 — ১০টি আলাদা রিপোর্ট (নতুন ফিচার)

`/reports` — কার্ড-মেনু, প্রতিটি রিপোর্টের নিজস্ব পেজ `/reports/<kind>` (ডিপ-লিংকযোগ্য)।
ওয়ার্কফ্লো: **ফিল্টার নির্বাচন → রিপোর্ট তৈরি → প্রিভিউ → আলাদা A4 PDF**।

| # | রিপোর্ট | ফিল্টার | শিট/PDF-এ যা থাকে |
|---|---------|---------|--------------------|
| ১ | বিক্রি | তারিখ থেকে/পর্যন্ত, পণ্য, ক্রেতা, নগদ/বাকি | তারিখ, পণ্য, ক্রেতা, ধরন, পরিমাণ, দর, বিক্রয় মূল্য + মোট |
| ২ | ক্রয় | তারিখ, পণ্য, সাপ্লায়ার | তারিখ, পণ্য, সাপ্লায়ার, পরিমাণ, ক্রয় দর, ক্রয় মূল্য + মোট |
| ৩ | স্টক | তারিখ (ঐচ্ছিক), পণ্য, ক্যাটাগরি | ওপেনিং স্টক, মোট ক্রয়, মোট বিক্রয়, বর্তমান স্টক, ক্রয় মূল্য, বিক্রয় মূল্য |
| ৪ | ক্রেতার বাকি | তারিখ (ঐচ্ছিক), ক্রেতা | বাকিতে বিক্রি, আদায়, বর্তমান বাকি (ক্রেতাভিত্তিক) |
| ৫ | বাকি আদায় | তারিখ, ক্রেতা, পেমেন্ট পদ্ধতি | তারিখ, ক্রেতা, পদ্ধতি, টাকা + মোট |
| ৬ | খরচ | তারিখ, খাত, ধরন (দোকান/মালিক) | তারিখ, খাত, বিবরণ, টাকা + মোট |
| ৭ | দৈনিক লাভ | এক তারিখ | বিক্রি, ক্রয়মূল্য, গ্রস লাভ, খরচ, নিট লাভ, নগদ/বাকি বিক্রি, আদায় |
| ৮ | মাসিক লাভ | মাস | বিক্রি, ক্রয়, ক্রয়মূল্য, গ্রস/নিট লাভ, বাকি বিক্রি, আদায়, সমাপনী বাকি, স্টক মূল্য |
| ৯ | পণ্য | ক্যাটাগরি, সার্চ | ক্রয় দর, বিক্রয় দর, ওপেনিং স্টক, বর্তমান স্টক |
| ১০ | লেনদেন | তারিখ, ধরন, সার্চ | তারিখ, ধরন, পণ্য, ক্রেতা/সাপ্লায়ার, পরিমাণ, টাকা, পেমেন্ট অবস্থা |

**PDF ফরম্যাট (প্রতিটি রিপোর্ট আলাদা ফাইল — কখনও একত্রে নয়)**
- hেডার: ব্যবসার নাম (শাখার organization) • রিপোর্টের নাম • নির্বাচিত সময়/মাস • ফিল্টারের সারসংক্ষেপ
- বডি: পরিচ্ছন্ন টেবিল (সঠিক কলাম অ্যালাইনমেন্ট), সারসংক্ষেপ স্ট্রিপ, **স্বয়ংক্রিয় টোটাল সারি**, দরকারি নোট
- ফুটার: ব্যবসার নাম • তৈরি তারিখ-সময় • **পৃষ্ঠা X / Y**
- অপ্রয়োজনীয় কলাম নেই — প্রতিটি রিপোর্টে ঠিক তার নিজের তথ্যই (টেস্টে যাচাই করা)
- পেজ ভাঙা: A4-এ multipage, কাটার আগে কাছাকাছি সাদা সারি খোঁজা হয় যাতে টেবিলের সারি মাঝখানে না কাটে
- ফাইল নাম: `sales-report-2026-09-01-2026-09-30.pdf` ধরনের (রিপোর্ট অনুযায়ী)

**ইমপ্লিমেন্টেশন**
- `src/lib/reports/core.ts` — ১০টি রিপোর্টের ডেফিনিশন, ফিল্টার স্পেক, বাংলা ফরম্যাটিং, শেয়ার টেক্সট
- `src/lib/reports/builders.ts` — রিপোর্টের হিসাব (pure ফাংশন, শাখা-স্কোপসহ)
- `src/lib/reports/pdf.ts` — A4 PDF (canvas → jsPDF, পেজ নম্বর স্ট্যাম্প) ও প্রিন্ট ডকুমেন্ট
- `src/lib/reports/useReportData.ts` — স্টোর + IndexedDB একত্রে, স্কোপ বসানো
- `src/components/report/*` — ReportSheet (A4 DOM), ReportFilters, ReportPreview, QuickSummary
- ডাউনলোড: পপ-আপ ছাড়াই আসল `.pdf` ফাইল সেভ; প্রিন্ট: নতুন ট্যাব, পপ-আপ আটকালে অ্যাপের ভিতরে প্রিভিউ; WhatsApp: Web Share API-তে PDF ফাইল, না থাকলে ডাউনলোড + wa.me
- পুরোনো ৩-ট্যাব সারসংক্ষেপ উপরে চাপা (`details`) অবস্থায় রাখা হয়েছে

---

## 📄 রিপোর্ট সেন্টার v1 (বিক্রি ও বাকি আদায় — ট্যাব সংস্করণ)

PRD মডিউল #১৫ (Report Center) বাস্তবায়ন: `/reports` — মালিক ও কর্মচারীর জন্য,
ক্রেতার জন্য নয় (`RoleRoute`)।

| রিপোর্ট | কী দেখায় |
|---------|-----------|
| **বিক্রি** | মোট/নগদ/বাকি বিক্রি, বিল সংখ্যা, গ্রস লাভ, গড় বিল, দৈনিক হিসাব, পণ্যভিত্তিক, ক্রেতাভিত্তিক, শাখা ও কর্মচারীভিত্তিক ব্রেকডাউন, বিলের বিবরণ |
| **বাকি আদায়** | মোট/গড়/সর্বোচ্চ আদায়, দৈনিক, ক্রেতাভিত্তিক, পেমেন্ট পদ্ধতি, শাখা/কর্মচারী ব্রেকডাউন, আদায়ের বিবরণ |
| **বাকি তালিকা** | বর্তমান বাকি (তারিখ-সীমাহীন): মোট বাকি, বাকিওয়ালা ক্রেতা, ফোন, শেষ আদায়ের তারিখ, শাখাভিত্তিক বাকি |

- **ফিল্টার:** আজ / গতকাল / শেষ ৭ দিন / এই মাস / গত মাস / কাস্টম তারিখ + শাখা (মালিক সব শাখা দেখেন, কর্মচারী শুধু নিজের শাখা)।
- **এক্সপোর্ট:** multi-page A4 **PDF**, **প্রিন্ট** (অ্যাপের স্টাইল কপি করে, তাই অফলাইনেও ঠিক দেখায়), **WhatsApp** — মোবাইলে PDF ফাইল সরাসরি শেয়ার হয় (Web Share API), সাপোর্ট না থাকলে PDF ডাউনলোড + `wa.me` টেক্সট।
- **নতুন ফাইল:** `src/lib/report.ts` (pure হিসাব), `src/lib/reportExport.ts` (PDF/প্রিন্ট/শেয়ার), `src/pages/Reports.tsx`, `tests/report.test.ts` (৭টি নতুন টেস্ট)।
- **লাভ-ক্ষতি পেজ** এখন একই এক্সপোর্ট হেল্পার ব্যবহার করে (আগে লম্বা রিপোর্ট এক পেজে চেপে যেত) + প্রিন্ট বাটন।
- **বান্ডল:** `html2canvas`/`jsPDF` সব জায়গায় dynamic import — মূল JS 1076 kB → **482 kB** (অডিট সুপারিশ #২ পূরণ)।
- **বাকি আদায়ের সোর্স:** নতুন লেজার এন্ট্রি (`kind: 'payment'`) + পুরোনো `collections` টেবিল (বাতিল এন্ট্রি বাদ)।
- **যাচাই:** `npm test` (২৫/২৫), `npm run lint` (০ এরর), `npm run build` ✅, এবং happy-dom দিয়ে পেজ রেন্ডার স্মোক-রান — বিক্রি/আদায়/বাকি তিন ট্যাব, তারিখ প্রিসেট, মালিক-বনাম-কর্মচারী স্কোপ মিলিয়ে দেখা হয়েছে (১৯টি চেক পাস)।

---

## 🔴 নতুন সমালোচনামূলক / উচ্চ সমস্যা (এই ব্রাঞ্চে ফিক্সড)

### N1. ডিপেন্ডেন্সি কনফ্লিক্ট — প্রজেক্ট ইনস্টল/বিল্ড/CI হতো না
**ফাইল:** `package.json`
**সমস্যা:** `vite@8.3.0` (dependabot আপগ্রেড) কিন্তু `@vitejs/plugin-react@^4.3.1` — যার peer range `vite ^4–^7`। ফলে `npm ci` ERESOLVE এররে ব্যর্থ; GitHub Pages ওয়ার্কফ্লো-ও ব্যর্থ হতো।
**ফিক্স:** `vite@^7.3.6` + `@vitejs/plugin-react@^5.2.0` (দুটোই স্টেবল, `vite-plugin-pwa@1.3.0`-এর সাথে সামঞ্জস্যপূর্ণ)। লকফাইল রিজেনারেট করা হয়েছে।

### N2. `npm run lint` ভাঙা — eslint ছিলই না
**সমস্যা:** `package.json`-এ `"lint": "eslint ."` কিন্তু eslint ইনস্টল/কনফিগ নেই।
**ফিক্স:** ESLint 9 flat config (`eslint.config.js`) + `typescript-eslint`, `react-hooks`, `react-refresh` প্লাগইন যোগ; ৪টি hooks-ওয়ার্নিং ফিক্স করে এখন **ক্লিন রান**।

### N3. টাকা ১০০ গুণ কম দেখাত — `MyDues.tsx` ডাবল-ডিভিশন বাগ
**ফাইল:** `src/pages/MyDues.tsx`
**সমস্যা:** `ledgerRows()` আগেই cents→টাকা কনভার্ট করে দেয়; পেজটি আবার `money(r.debit / 100)` করায় ক্রেতার বাকি খাতায় জমা/বাকি কলাম ১০০ গুণ কম দেখাত (ব্যালেন্স কলাম ঠিক ছিল)।
**ফিক্স:** `/100` সরানো হয়েছে — এখন `Collections.tsx`-এর মতোই সরাসরি `money(r.debit)`।

### N4. UTC তারিখ বাগ — "আজ/এই মাস"-এর হিসাব ভুল (BD টাইমজোনে রাত ১২টা–সকাল ৬টা)
**ফাইল:** `src/pages/Dashboard.tsx`, `src/pages/Sales.tsx`, `src/stores/salesStore.ts`, `src/pages/Stock.tsx`
**সমস্যা:** `new Date().toISOString().split('T')[0]` হলো **UTC** তারিখ; বাংলাদেশে (UTC+6) মধ্যরাত থেকে সকাল ৬টা পর্যন্ত "আজ"-এর বিক্রি, ড্যাশবোর্ড স্ট্যাটস, মাসের শুরু — সব আগের দিনের হিসাব দেখাত।
**ফিক্স:** বিদ্যমান লোকাল-ডেট হেল্পার `toDateKey()` ব্যবহার করা হয়েছে (Dashboard-এ `monthStart`-ও)।

### N5. ডেটা-মডেল অসামঞ্জস্য — বিক্রি/ক্রয়ের তারিখ UTC, লেজার এন্ট্রির তারিখ লোকাল
**ফাইল:** `src/pages/Sales.tsx`, `src/pages/Orders.tsx` (ডেলিভারি→বিক্রি), `src/pages/Purchases.tsx`, `src/lib/profitLoss.ts`
**সমস্যা:** লেজার এন্ট্রি `YYYY-MM-DD` (লোকাল) সেভ হয়, কিন্তু Sale/Purchase সেভ হতো UTC ISO-তে; ফলে একই দিনের লেনদেন দুই রকম দিন-কি পেত, `inRange`/ফিল্টারে অমিল।
**ফিক্স:** নতুন হেল্পার `nowLocalISO()` (লোকাল ক্যালেন্ডার দিন + সময়) — Sale, Purchase, order→sale কনভার্সনে ব্যবহৃত। পুরনো ডেটা পার্স/সর্টে অক্ষত থাকে।

### N6. ID collision — `Date.now()` ভিত্তিক আইডি
**ফাইল:** salesStore, purchaseStore, customerStore, stockAdjustmentStore, productStore, Expenses, Orders, Purchases (invoice_id)
**সমস্যা:** একই মিলিসেকেন্ডে দুটি এন্ট্রি হলে আইডি ক্ল্যাশ।
**ফিক্স:** সব জায়গায় `crypto.randomUUID()`। (`grep Date.now src/` → এখন খালি।)

### N7. রোল-ভিত্তিক রাউট প্রোটেকশন ছিল না
**ফাইল:** `src/App.tsx`
**সমস্যা:** `ProtectedRoute` শুধু লগইন দেখত; ক্রেতা রোল URL টাইপ করে `/sales`, `/stock`, `/purchases`-এ পৌঁছাতে পারত (পেজগুলোতে আলাদা গার্ড ছিল না)।
**ফিক্স:** নতুন `RoleRoute` কম্পোনেন্ট — `sales/purchases/collections/stock/profit-loss/expenses/customers` → owner+staff, `branch-pads` → owner, `my-dues` → customer; অনুমতি না থাকলে হোমে রিডাইরেক্ট।

### N8. PWA আইকন ৩ মেগাবাইট! — precache 4.2 MB, ইনস্টল/লোড ধীর
**ফাইল:** `public/*.png`
**সমস্যা:** প্রতিটি আইকন আসলে 1024×1024 আনঅপ্টিমাইজড PNG (pwa-192x192.png-ও 837 KB!)। মোট ~2.97 MB, workbox precache 4198 KiB।
**ফিক্স:** ImageMagick দিয়ে সঠিক মাপে রিসাইজ + কালার কোয়ান্টাইজ: apple-touch 180², favicon 64², logo 256², pwa-192, pwa-512 — মোট **~25 KB**। Precache এখন 1265 KiB (বাকিটা মূলত JS বান্ডল)।

---

## 🟠 মাঝারি সমস্যা (এই ব্রাঞ্চে ফিক্সড)

### M1. `supabase.ts` খালি URL-এ ক্লায়েন্ট তৈরি করত
**ফিক্স:** এনভ ভেরিয়েবল না থাকলে `supabase = null`, `isSupabaseConfigured` এক্সপোর্ট; আর রানটাইম ক্র্যাশ নেই।

### M2. `useLiveQuery(...) || []` প্রতি রেন্ডারে নতুন অ্যারে → useMemo ডিপেন্ডেন্সি অস্থির
**ফাইল:** Dashboard, Expenses, ProfitLoss — ESLint ওয়ার্নিং হিসেবে ধরা পড়ে; `useMemo`-র্যাপ করা হয়েছে।

---

## 🟢 পুরনো রিপোর্টের আইটেম — বর্তমান অবস্থা

| পুরনো আইটেম | বর্তমান অবস্থা |
|--------------|----------------|
| C1 নিরাপত্তাহীন মক লগইন | ✅ **রেজলভড:** Dexie-তে ইউজার + SHA-256 পাসওয়ার্ড হ্যাশ, ডেমো অ্যাকাউন্ট (01700000000 ইত্যাদি / 123456)। *নোট:* ক্লায়েন্ট-সাইড হ্যাশ — মাল্টি-ডিভাইস সিঙ্কের সময় সার্ভার-সাইড auth (Supabase) দরকার। |
| C2/C3 Supabase অব্যবহৃত | 🟡 এখনও লোকাল-ফার্সট; Supabase sync রোডম্যাপে। `supabase.ts` এখন নিরাপদ (N/M1)। |
| C4 npm install ব্যর্থ | ✅ ফিক্সড (N1) |
| H1 PWA আইকন অনুপস্থিত | ✅ আছে + অপ্টিমাইজড (N8) |
| H2 Dexie অব্যবহৃত | ✅ রেজলভড — auth, customers, ledger, expenses, orders এখন IndexedDB/Dexie-তে |
| H3 units_per_bag অব্যবহৃত | 🟡 এখনও ডিসপ্লে-অনলি; ব্যাগ↔কেজি কনভার্সন রোডম্যাপে |
| H4 PDF অব্যবহৃত | ✅ রেজলভড — লাভ-ক্ষতি PDF (ProfitLoss), লেজার রসিদ PDF/ইমেজ/প্রিন্ট (LedgerReceipt) |
| H5 customer store নেই | ✅ রেজলভড — customerStore + ক্রেতা লিঙ্কিং (customerLink) |
| M1 (পুরনো) stock validation নেই | ✅ রেজলভড — Sales-এ স্টক চেক + ওয়ার্নিং কনফার্ম |
| M2 (পুরনো) ID collision | ✅ ফিক্সড (N6) |
| M3 (পুরনো) Dashboard হার্ডকোডেড ০ | ✅ রেজলভড — রিয়েল স্ট্যাটস |
| S1 Purchases নেভিগেশন | ✅ "আরও" মেনুতে আছে |
| S2 Role-based route protection | ✅ ফিক্সড (N7) |

---

## 🔵 কাস্টমার মডিউল (v1.2) — স্মোক/টেস্টে যা ধরা পড়ে ঠিক করা হয়েছে

### C1. সব রিপোর্টের তারিখে হাজার-বিভাজক বসত — `bnDate` বাগ
`bnNum`-এর গ্রুপিং থাকায় ২০২৬ → "২,০২৬" হয়ে যেত (`১৫/৯/২,০২৬`), অর্থাৎ প্রতিটি রিপোর্ট PDF, ফুটার তারিখ ও স্টেটমেন্টে। ফিক্স: তারিখের অঙ্ক `useGrouping:false` দিয়ে বসানো হয় (`src/lib/reports/core.ts`)। টেস্ট: `tests/reports.test.ts`।

### C2. সরাসরি লিংকে ক্রেতার প্রোফাইল খুলত না — `/customers/:id` ডিপ-লিংক
পেজ শুধু Zustand স্টোরের তালিকা থেকে ক্রেতা খুঁজত; রিফ্রেশ/শেয়ার করা লিংকে স্টোর খালি থাকলে "এই ক্রেতা পাওয়া যায়নি" আসত। ফিক্স: `db.customers.get(id)` থেকে সরাসরি পড়া (`useLiveQuery`) + স্টোর fallback, আর লোড না হওয়া পর্যন্ত "লোড হচ্ছে…"।

### C3. বাংলা 'য়'-এর দুই রূপে ডুপ্লিকেট ক্রেতা — ইউনিকোড অসমতা
`য়` কখনো `U+09DF`, কখনো `U+09AF + U+09BC` হিসাবে লেখা থাকে; সাধারণ `===` তুলনায় একই নাম দুইবার যোগ হত। ফিক্স: `normName()` (NFC + space collapse + lowercase) — ক্রেতা ডুপ্লিকেট-চেক ও অনুমোদনে ব্যবহৃত।

### C4. `/profile` পেজে শিরোনাম ছিল না
কাস্টমার-প্রোফাইল অগ্রাধিকার হিসেবে "প্রোফাইল" শিরোনাম + ভূমিকা/ফোন লাইন যোগ করা হয়েছে (a11y-friendly `h1`)।

**নতুন টেস্ট:** `tests/customer.test.ts` (সাইন-আপ/অনুমোদন গেট, স্টেটমেন্ট হিসাব, তাগাদা/বার্তার টেক্সট, WhatsApp লিংক, প্রোফাইল ও পাসওয়ার্ড, বার্তা টেবিল) এবং `npm run test:ui` (৭টি পেজের রেন্ডার স্মোক, ১৭টি অ্যাসার্শন)।

---

## 💡 বাকি সুপারিশ (রোডম্যাপ)

1. **Supabase sync** — বর্তমানে ডাটা ডিভাইস-লোকাল (Dexie + zustand persist)। মাল্টি-ডিভাইস/ব্যাকআপের জন্য sync লেয়ার যোগ করুন।
2. ~~**বান্ডল সাইজ**~~ — ✅ **সম্পন্ন:** jsPDF + html2canvas dynamic import-এ সরানো হয়েছে; মূল JS 1076 kB → **482 kB** (রিপোর্ট সেন্টার সেকশন দেখুন)।
3. **সার্ভার-সাইড auth** — ক্লায়েন্ট-সাইড SHA-256 শুধু ডেমো/লোকাল-ফার্সট ব্যবহারের জন্য যথেষ্ট; প্রোডাকশন সিঙ্কে OTP/পাসওয়ার্ড Supabase Auth-এ নিন।
4. **ইউনিট কনভার্সন** — বস্তা↔কেজি (`units_per_bag`) এখনও হিসাবে ব্যবহৃত নয়।

---

## 🎯 ভেরিফিকেশন কমান্ড

```bash
npm ci          # ✅ ক্লিন ইনস্টল
npm run lint    # ✅ 0 errors, 0 warnings
npm run build   # ✅ tsc + vite, মূল JS ~500 kB, precache 1338 KiB
npm test        # ✅ 57/57
npm run test:ui # ✅ 17/17 (কাস্টমার-মডিউল UI স্মোক)
npm audit       # ✅ 0 vulnerabilities
```

---

*অডিট + ফিক্স সম্পন্ন: ১৮ সেপ্টেম্বর ২০২৬ — লাইভ ডিপ্লয়: GitHub Pages (নিচে Deploy সেকশন দেখুন)*
