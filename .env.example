# Server-side master admin authentication.
# Keep the real values in Vercel/project secrets or an untracked .env.local file.
SHOPLEDGER_MASTER_ADMIN_ID=
SHOPLEDGER_MASTER_ADMIN_PASSWORD=
# Use a random value with at least 32 characters to sign the HTTP-only session cookie.
SHOPLEDGER_SESSION_SECRET=
