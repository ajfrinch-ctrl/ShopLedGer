# ShopLedGer — পরিবর্তন লগ

## 2026-09-20 (রাত) — লাইভ টপবার: ফিক্সড, বার-তারিখ-ঘড়ি ও আবহাওয়া-ভিত্তিক রঙ

- **টপবার ফিক্সড:** `sticky` → `position: fixed`; উচ্চতা `ResizeObserver` দিয়ে মেপে `--topbar-h` CSS ভেরিয়েবলে বসে,
  `<main>` ততটুকু জায়গা ছেড়ে শুরু হয় (নচ/সেফ-এরিয়া ও ফন্ট লোডের পরেও ঠিক থাকে)। টোস্টও টপবারের নিচে নামে।
- **আজকের বার, তারিখ ও সময়:** টপবারের নিচের স্ট্রিপে «রবিবার, ২০ সেপ্টেম্বর» ও চলমান ঘড়ি «রাত ৯:২৪:২৬»
  (বাংলা অঙ্ক, ভোর/সকাল/দুপুর/বিকেল/সন্ধ্যা/রাত, দোকানের সময়াঞ্চল Asia/Dhaka — ডিভাইসের TZ যা-ই হোক)।
  ট্যাব আড়ালে গেলে টিক বন্ধ থাকে, ফিরলে সাথে সাথে সঠিক সময়।
- **আবহাওয়া অনুযায়ী রঙ (অটো):** দোকানের অবস্থানের (আমুচিয়া, বোয়ালখালী) বর্তমান আবহাওয়া
  [Open-Meteo](https://open-meteo.com) থেকে (বিনামূল্যে, API key নেই) — WMO কোড + দিন/রাত + তাপমাত্রা →
  ১৪টি রঙের মুড (রোদ, প্রখর রোদ, পরিষ্কার রাত, আংশিক মেঘলা, মেঘলা, কুয়াশা, বৃষ্টি, বজ্রঝড়, তুষার, ভোর, গোধূলি…)।
  ব্রাউজার/PWA-এর `theme-color`-ও সাথে বদলায়। রঙ বদল মসৃণ (৭০০ms ট্রানজিশন)।
  - টপবারে আবহাওয়া চিপ (আইকন + তাপমাত্রা); চাপলে মেনুতে বিস্তারিত (অনুভূত তাপমাত্রা, আর্দ্রতা, কখন আপডেট) ও রিফ্রেশ বোতাম।
  - **অফলাইন/এপিআই ব্যর্থ:** ৩ ঘণ্টা পর্যন্ত localStorage ক্যাশ; তারপর দিনের সময় অনুযায়ী রঙ (ভোর/দিন/গোধূলি/রাত)।
  - ৩০ মিনিট পরপর রিফ্রেশ; ট্যাবে ফিরলে বা অনলাইন হলে বাসি থাকলে আবার নামায়।
  - মেনুতে টগল «আবহাওয়া অনুযায়ী টপবারের রঙ» — বন্ধ করলে আগের সাধারণ মিন্ট রঙ (পছন্দ `shopledger-ui-prefs`-এ সংরক্ষিত)।
- **নতুন ফাইল:** `src/components/top-bar.tsx`, `src/lib/topbar-theme.ts` (pure লজিক), `src/lib/weather.ts`,
  `src/lib/use-weather.ts`, `src/lib/ui-prefs.ts`; `SHOP.location` (`src/lib/shop.ts`)।
- **টেস্ট:** `scripts/topbar-theme.test.mjs` — ১৪টি টেস্ট (মুড ম্যাপিং, প্যালেট, বাংলা ঘড়ি/তারিখ TZ-নিরপেক্ষ,
  Open-Meteo পার্সিং, ক্যাশ)। `npm test` মোট ৮৮ টেস্ট, ০ ফেল।

## 2026-09-20 (রাত) — গ্রোকমুক্ত রিপো + পূর্ণাঙ্গ GitHub প্রজেক্ট সেটআপ

প্রজেক্টটি এখন সম্পূর্ণভাবে **গ্রোকমুক্ত** এবং একটি স্ট্যান্ডার্ড GitHub প্রজেক্ট হিসেবে সাজানো।

- **গ্রোক-সম্পর্কিত সব বাদ:**
  - `.grok/` ডিরেক্টরি ও `app-env` র‍্যাপার (`with-app-env`) — বিল্ড/ডেভ এখন সরাসরি `vite` চালায়।
  - গ্রোক পিডব্লিউএ/ব্র্যান্ডিং স্তর: `scripts/grok-pwa-*`, `scripts/install-page.html`, `scripts/brand-check.mjs`, `server/middleware/grok-pwa.ts` — পিডব্লিউএ ম্যানিফেস্ট ও আইকন এখন সাধারণ `public/manifest.webmanifest` + `public/icon-180.png`।
  - গ্রোক প্রিভিউ ব্রিজ ও এম্বেডার (`preview-host-bridge`, `preview-embedder-origin`) — `__root.tsx` এখন শুধু অ্যাপ রেন্ডার করে।
  - গ্রোক কানেক্টর/অ্যাপ-ডাটা স্ক্যাফোল্ড (`src/lib/app-data/`) — অ্যাপ নিজস্ব স্টোর ও PGLite ডাটাবেজেই চলে।
  - ফেডারেটেড (গ্রোক-ব্রোকার) অথ স্ট্যাক (`src/lib/auth/`, `better-auth` নির্ভরতা, `migrations/auth`) — লগইন এখন অ্যাপের নিজস্ব ভূমিকাভিত্তিক সিস্টেমেই হয়।
  - `AGENTS.md`, `public/__grok/`, গ্রোক-সম্পর্কিত স্ক্রিনশট ও `README`/`CHANGELOG` উল্লেখ।
- **নতুন/পরিষ্কার ফাইল:** `public/manifest.webmanifest`, `public/icon-180.png` (পূর্বে `public/__grok/icon-180.png`), `.gitignore`-এ `.cache/`।
- **নির্ভরতা:** `better-auth`, `jose`, `kysely` বাদ; `package.json`-এর নাম `shopledger`।
- **GitHub প্রজেক্ট সেটআপ:** নতুন CI ওয়ার্কফ্লো (`.github/workflows/ci.yml` — লিন্ট + টাইপচেক + টেস্ট + বিল্ড), ইস্যু টেমপ্লেট (বাগ রিপোর্ট / ফিচার রিকোয়েস্ট), পুল-রিকোয়েস্ট টেমপ্লেট, `CONTRIBUTING.md` ও `LICENSE` (সর্বস্বত্ব সংরক্ষিত)।
- **যাচাই:** `npm run lint` ✅ • `npm run typecheck` ✅ • `npm test` (৭৪ টেস্ট, ০ ফেল) ✅ • `npm run build` ✅ • `npm run dev` → HTTP 200, `lang="bn"` HTML, ম্যানিফেস্ট ও আইকন সার্ভ হয়।

## 2026-09-20 (সন্ধ্যা) — অ্যাপ পুনর্গঠন (TanStack স্ট্যাক)

- **অ্যাপ বদল:** কর্ণফুলী সেলস সেন্টার অ্যাপটি **TanStack Start + Nitro + PGLite** স্ট্যাকে পুনর্গঠিত হয়ে রিপোর মূল অ্যাপ হিসেবে বসানো হলো; পুরনো Vite+React PWA-এর ফাইল সরানো হলো (ইতিহাস ও ডকুমেন্টেশন `docs/`-এ সংরক্ষিত)।
- **ডিপ্লয়:** লাইভ টার্গেট **Vercel** — ব্রাঞ্চ পুশে Preview, `main`-এ Production। অ্যাপটি SSR-নির্ভর হওয়ায় স্ট্যাটিক হোস্টিংয়ে চলে না।
- **ডাটাবেজ:** ডিফল্ট এম্ববেডেড PGLite; `DATABASE_URL` দিলে পোস্টগ্রেস/Neon।

## 2026-09-20 — অ্যাপ ফিচার আপডেট

- **MyDues (ক্রেতার হিসাব):** ইংরেজি শিরোনাম/বোতাম বাংলায় করা হয়েছে।
- **ভূমিকা গার্ড:** পেজের ভিতরে `role === 'customer'` না হলে স্টাফের জন্য আপনা ক্রেতা-খাতা তৈরি হয় না।
- **সেলস ম্যান তালিকা:** আইডি সংখ্যা বাংলা অঙ্কে (`২টি আইডি`)।
- **লক বনাম নিষ্ক্রিয়:** নিষ্ক্রিয় আইডিতে «নিষ্ক্রিয়» দেখায়; «লক (ভুল পাসওয়ার্ড)» শুধু ৫ বার ভুল লগইনে।
- **শাখা প্যানেল:** খুললেই প্রথম সক্রিয় শাখা স্বয়ংক্রিয় নির্বাচিত হয় — খালি ফর্ম ও `(0)` আইডি দেখায় না।

---

রিপো: `https://github.com/ajfrinch-ctrl/ShopLedGer` • লাইভ: Vercel
